spring:
  profiles: prod
spring:
  datasource:
    url: jdbc:mysql://mysql-booking:3306/booking_service
    driverClassName: com.mysql.cj.jdbc.Driver
    username: ${BOOKING_DB_USERNAME:booking}
    password: ${BOOKING_DB_PASSWORD:password}
